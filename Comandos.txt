$ git init

$ git config user.name "YessiGM"

$ git config user.email "yessi.galicia@uabc.edu.mx"

$ git remote add origin "https://github.com/YessiGM/Juego.git"

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

git status -------Miras que archivos faltan actualizar en repositorio local y cuales estan subidos

git add (Nombre del archivo) -----agregar archivos 

git commit -m ("Mensaje o comentario") ----- confirmas los archivos y agregas mensaje o comentario----------------

git push -u origin (Nombre de la rama) ---para subir a github los archivos

git pull origin (nombre de la rama) ----para actualizar archivos locales

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

git merge nombreC nombreD

git branch -----Para ver la lista de ramas 

git bracnh (nombre) ---- creas rama 

git branch -d (nombre) ------ Eliminas la rama 

------------------------------------------------------------------------------------------------------------------------------------------------------








